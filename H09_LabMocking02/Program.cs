using System;

namespace H09_LabMocking02
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             
             * An Email has a From, a To, a Subject and a Body.
             * An Email can be sent by passing an EmailService.
             * 
             * An EmailService cand Send and Receive Emails.
             * 
             * Right now the Email.Send is not unit testable.
             * Refactor the project to be able to 
             * test the Email by mocking the EmailService.
             * 
             */

            Console.ReadLine();
        }
    }
}
